import { ImageTrackDirective } from './image-track.directive';

describe('ImageTrackDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageTrackDirective();
    expect(directive).toBeTruthy();
  });
});
